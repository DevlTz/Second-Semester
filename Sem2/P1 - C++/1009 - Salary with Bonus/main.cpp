#include <bits/stdc++.h>

int main(int argc, char* argv[]){
    double fixc {}, bonus {};
    char name[30];
    std::cin.getline(name, 30);
    std::cin >> fixc >> bonus;
    double final = 
    std::cout << std:setprecision(4)
    std::cout << "TOTAL = " << 
}